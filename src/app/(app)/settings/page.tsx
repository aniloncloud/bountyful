import { AppDownloadRedirect } from "@/components/AppDownloadRedirect";

export default function SettingsPage() {
  return (
    <AppDownloadRedirect
      title="Settings"
      description="Manage your account settings, privacy preferences, and notifications in the Bountyful mobile app."
    />
  );
}
